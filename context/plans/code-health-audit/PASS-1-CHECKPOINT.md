# Pass 1 Checkpoint — Code Health Audit 2026-06-25

**Target:** PerformanceProfiler — tModLoader 1.4.4 / .NET 8 C# mod, v0.22.0, ~43.3k production LOC.
**Supersedes** the 2026-06-22 audit in this folder: that run targeted the hook-install RAM path, the broken test build, and doc rot. **Most of its findings have since landed** — `MarkInstallEnd` now forces a Gen2 + `TrimRetainedScaffolding` reclaims scaffolding (RAM 3.7→1.0 GB per `install-ram-optimisation.md`); the test csproj paths are repaired (suite builds + passes); so this is a fresh audit against current reality, not a re-run.

## 1. Context read (Pass 1 step 1-3)

Read this session (upkeep-context immediately prior): `context/architecture.html` (`arch/data.js`), `notes.md`, all 14 `systems/*.md` (freshly reconciled to v0.22), `integration/integration-map.md`, `notes/{decisions,conventions,philosophy,compile-gate,insights-rework-status}.md`, `plans/install-ram-optimisation.md`. The architecture is fully internalised from the upkeep pass.

## 2. Stack + script coverage

C# / .NET 8. `file_size_scan.py` is language-agnostic → **used**. `modularisation_candidates.py` / `import_graph.py` / `hotspot_intersect.py` / `orphans.py` are Py/Rust-only → **fallback** to `file_size_scan.py` + `Grep` (call-site / dead-code survey) + `git log --name-only` (churn). `test_baseline.sh` detects Cargo/pyproject/package.json/go.mod, not .NET → **fallback** to `dotnet test` + `dotnet msbuild` directly. Recorded as reasoned omissions in `obligation-evidence-map.md`.

## 3. Test + build baseline (Pass 1 step 5)

| Gate | Command | Result |
|---|---|---|
| Compile (full mod) | `dotnet msbuild PerformanceProfiler.csproj -v m -nologo \| grep -cE 'error CS'` | **0 error CS** (clean mod-wide; TML003 packaging lock n/a — game not holding the .tmod this run) |
| Pure-logic suite | `dotnet test Tests/PerformanceProfiler.Tests.csproj` | **108 passed / 0 failed / 0 skipped, 22 s** |

No pre-existing failures → no Known-Issues findings from the baseline. The suite is healthy and is a usable verification ground for diagnostic tests this audit writes.

## 4. Modularisation candidate list (Modularisation evaluation floor)

C# threshold 550; candidate = ≥550 LOC **or** top-decile by line count. From `file_size_scan.py`. Generated/vendored/tooling files (`context/arch/*.js` — generated by the arch pipeline; `tools/testing/*.py` — off-game harness) are **not production source** → not-applicable. Archived `UI/` overlay is compiled-out → not-applicable.

| File | LOC | Qualifies | Audit cluster |
|---|---|---|---|
| `Profiling/HookInterceptor.cs` | 1227 | threshold + top-decile | Hook instrumentation |
| `Web/Assets/Js/Js.Timeline.cs` | 808 | threshold | Web JS (UI) |
| `UI/Overlay/Tabs/OverviewTab.cs` | 786 | threshold | **not-applicable** (archived overlay, compiled-out) |
| `Profiling/ProfilerSystem.cs` | 753 | threshold | Mod lifecycle |
| `Data/Streams/SessionRecorder.cs` | 737 | threshold | Persistence |
| `Profiling/ILHookInterceptor.cs` | 706 | threshold | Hook instrumentation |
| `UI/Overlay/OverlayPanel.cs` | 679 | threshold | **not-applicable** (archived overlay) |
| `Web/Assets/Js/Js.Insights.cs` | 676 | threshold | Web JS (UI) |
| `Web/Assets/Js/Js.Lag.cs` | 629 | threshold | Web JS (UI) |
| `Data/Detectors/StallDetector.cs` | 594 | threshold | Metric/detectors |
| `Profiling/MetricCollector.cs` | 586 | threshold | Metric/detectors |
| `Data/Contracts/RolloutContracts.cs` | 560 | threshold | Data pipeline |
| `Data/Aggregators/Segments/SegmentDetector.cs` | 550 | threshold | Data pipeline |

Per-file verdicts are produced in Pass 2 (`PASS-2-SYSTEMS-AUDITED.md`). No "out-of-scope" verdict is permitted; each gets split-recommended / leave-as-is / not-applicable.

## 5. Systems + Pass-2 prioritisation

Production trees: `Profiling/` 89, `Data/` 68, `Web/` 61, `Insights/` 42, `UI/` 33 (archived). Prioritised by substantive-work density + hot-path proximity + recent churn (the v0.13→v0.22 arc touched Insights/ + Web/Assets heavily):

| # | System cluster | Files | Why prioritised |
|---|---|---|---|
| 1 | **Hook instrumentation + self-health** | `HookInterceptor` (1227), `ILHookInterceptor` (706), `HookCategoryRouter`, `HookBackend`, `ProbeStack`, `HookSurfaceCache`, `ProfilerSelfHealth` | The per-tick hot path (Invariant 2) + the RAM lever; largest file in the repo; prior audit's focus, now partly reworked |
| 2 | **Metric collection + detectors** | `MetricCollector` (586), `RingBuffer`, `StallDetector` (594), `SpikeDetector`, `PerModAttribution`, `PerTickAttributionRing` | Per-tick frame engine; zero-alloc contract; ring-buffer + detector hot loops |
| 3 | **Data pipeline + segments** | `DataRegistry`, `RolloutContracts` (560), `SegmentDetector` (550), aggregators, stats | The calculation locus; every number; frozen per-tick callbacks |
| 4 | **Insights engine** | `Insights/*` (42 files: engine, 16 detectors, reference frames, drivers, publish stats) | Newest subsystem (v0.19 rework); statistical off-thread path (~60 ticks); Welford/stats math |
| 5 | **Web server + routers + persistence** | `DashboardHttpServer`, `DashboardRouter.*` (8 partials), `SessionRecorder` (737), `ProfilerDatabase`, `DbWriterThread` | The HTTP worker path + the LiteDB writer thread; snapshot serialisation |
| 6 | **Web JS/CSS assets (UI/UX)** | `Js.Timeline` (808), `Js.Insights` (676), `Js.Lag` (629), `Js.Summary`, `Js.Charts`, `Js.Memory`, `Css.*` | The player surface; user flagged UI/UX files for extra verification care; per-render JS work |
| — | **Cross-cutting hygiene** | dead code, duplicate-using, doc rot, dependency hygiene, config drift | Repository-wide sweep |
| — | UI/ archived overlay | `UI/Overlay/*` (33) | **not-applicable** — compiled-out; modularisation not-applicable verdicts only |

## 6. Known issues carried in from context

- Prior audit P-1 (per-hook Cecil ILContext retention) — partly addressed by `TrimRetainedScaffolding` (Step 2 live); the deeper "own the emission" Step 3 is unstarted (tracked in `install-ram-optimisation.md`). Re-assess in cluster 1.
- The insight feed has no player UI; the per-insight LiteDB collection has a writer scaffold but no producer (found during upkeep) — a Known-Issues candidate for cluster 4.
- Source-comment drifts flagged in `_Overview` (F10-vs-F9 comments; KpiStat doc-comment registration site) — doc-rot candidates for cross-cutting.

Pass 2 proceeds: per-cluster deep dive with mode-varied WebSearch research + diagnostic tests where they resolve uncertainty.
